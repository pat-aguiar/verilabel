# ADR-005: Frontend Stack

**Status:** Accepted

## Context
Need a responsive dashboard that can visualize "Pass/Fail" states clearly.

## Decision
Use Tailwind CSS for rapid UI building. We will implement a Component-Driven Design for the "Transparency Badge" so it can eventually be exported as an iframe or a standalone web component for CPG brands to embed on their own sites.